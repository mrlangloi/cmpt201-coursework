#define _POSIX_C_SOURCE 200809L
#include <string.h>
#include <unistd.h>

int EXTRA_SIZE = 256;

int main() {
  int waldo = 42;

  write(STDOUT_FILENO, "%p\n", strlen((char *)waldo));

  // increase heap size
  // int *mem_loc = (int *)sbrk(EXTRA_SIZE);

  // initialize block 1

  // initialize block 2

  // print

  return 0;
}

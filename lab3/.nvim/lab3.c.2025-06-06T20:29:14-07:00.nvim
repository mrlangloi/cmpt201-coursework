#define _POSIX_C_SOURCE 200809L
#include <stdio.h>
#include <stdbool.h>

int main() {

  char *line;
  size_t size = 0;

  while (true) {

    ssize_t len = getline(&line, &size, stdin);
  }

  return 0;
}
